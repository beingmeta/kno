#!/bin/sh
EXTMODS=$(cat ./extmods 2>/dev/null)
echo "Args=$*";
for dir in ${EXTMODS}; do
    if [ -f ${dir}/.skip ]; then
	:;
    elif [ -d ${dir}/.git ]; then
	("cd" ${dir};
	 echo "# In ${dir}"
	 "$@";)
    fi;
done
