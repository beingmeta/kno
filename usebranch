if [ ! $# -eq 0 ] && [ $1 != "current" ]; then
   git checkout $1;
   shift;
elif [ ! $# -eq 0 ] && [ $1 == "current" ]; then
   shift;
fi;
if test -f makefile; then
   make distclean;
fi;
./configure $*
make -j
